[env:m5stack-unitc6l]
; pioarduino 分支提供最新 ESP32-C6 支援
platform  = https://github.com/pioarduino/platform-espressif32/archive/refs/heads/develop.zip
board     = esp32-c6-devkitc-1
framework = arduino

upload_speed  = 1500000
monitor_speed = 115200

; 啟用 USB CDC（攜帶模式 USB Serial 必須）
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DCORE_DEBUG_LEVEL=3

; 分區表（16MB Flash，default_16MB.csv 內含 app0/app1 雙 OTA 分割區）
board_build.partitions = default_16MB.csv
; Unit C6L 實機為 16MB Flash（board 預設標 8MB，需明確指定以利 OTA 寫入 app1）
board_upload.flash_size = 16MB
board_build.flash_size  = 16MB

lib_deps =
    ; SX1262 LoRa 驅動
    jgromes/RadioLib @ ^6.6.0
    ; M5Stack 硬體抽象（OLED、LED、按鈕）
    m5stack/M5Unified @ ^0.2.2
    ; ArduinoJson（設定指令解析）
    bblanchon/ArduinoJson @ ^7.1.0
